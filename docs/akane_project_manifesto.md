# AkaneCompanionLab — 项目自述

> 一份给我自己、给未来 AI 协作者、给任何想了解这个项目是什么的人的入门文档。
>
> 不是 README（讲怎么装），不是设计文档（讲怎么实现），是讲**这个项目究竟在做什么**。
>
> 写于 2026-06-16，三次深度设计会话沉淀之后。

---

## 一句话

**AkaneCompanionLab 是一个让用户和 AI 角色一起塑造一段相处关系的桌面伴侣项目。**

我做的不是更聪明的 AI——市面上已经有很多。我做的是让"相处"这件事变得**更轻松、更日常、更有新鲜感**的小世界。

## 它是什么 / 它不是什么

| 它不是 | 它是 |
|---|---|
| Chatbot（问答式工具） | 桌面上一起生活的伴侣 |
| Character.ai 风格的角色 playground | 用户和角色一起塑造的关系 |
| VTuber overlay / 桌面装饰 | 真正参与你日常的存在 |
| AI 工具箱（能力多但抓不住人） | 抓住人的小世界 |
| "看 AI 多厉害"的展示型 demo | "看我和她一起生活什么样"的切片 |

**一句话边界**：我不在做"展示 AI 能力"的项目，我在做"让用户和 AI 共度时光"的项目。

## 核心机制：环境字典 + 动态提示词编排

这是这个项目运转的根本机制。理解了它，你就理解了整个项目。

### 环境字典

用户上传 / 选择的资源——**就是 Akane 当下"能感知什么 / 能表达什么"的边界**：

- 表情图集 → 她能露出的情绪
- 服装包 → 她能呈现的形态
- BGM 库 → 你们能一起听的歌
- 背景图 → 她所处的"小小世界"
- 已启用工具 → 她现在能做的事
- 关系记忆 → 她记得跟你的什么事

**关键性质**：删一个表情，她真的就不能那样了；加一首歌，她就多一段共听的可能。

这个机制是项目作者亲自撞出来的——我曾经发现我做的角色不会生气，后来才意识到，是因为**我没给她生气的表情图**。从那以后我才明白：**角色不是一句设定写完就结束了，环境也是她的一部分**。

### 动态提示词编排

每轮对话之前，系统按"当下场景"拼装 prompt：

- 在听歌？→ 当前歌曲 + 歌词片段塞 prompt
- 当前可选表情？→ 显式告诉模型可选范围
- 看到桌面活动？→ 视觉描述塞 prompt
- 心情切换？→ 对应参考音 + 立绘 overlay
- 当前可用工具？→ 当前 surface 的工具描述塞 prompt

模型在这个动态环境里产生**不被代码穷举的反应**。**规则在框架里，花样在每次注入的细节里**。

### 三者循环

```
用户 → 塑造 → 环境字典 → 供给 → 动态编排 → 驱动 → Akane 的反应 → 体验 → 用户继续塑造
```

用户塑造环境，环境塑造她，她塑造体验，体验让用户继续塑造。**这个循环就是项目本体**。

## 6 条核心设计原则

不可妥协的：

1. **UI 服务于陪伴，不是展示能力**
   控制中心不是"她的后台"，是"我们一起经营生活的地方"。

2. **自由由相处构建，不在程序里写死**
   能力不是预设清单，是慢慢长出来的；用户能添加，她也能学习获取。

3. **任何反馈通过她，不通过 toast / 弹窗**
   拖一段音频给她？她看向音频、眨眨眼、说"一起听吗"。不是弹"导入成功"。

4. **UI 操作 = 第三交互通道**
   点击 / 拖拽 / 编辑 UI 跟"打字 / 语音"平级，都是和她相处的方式。

5. **"如何被表现"是功能验收硬指标**
   做了功能但表现不到位 = 没做。最难受的不是没做，是**做了又像没做**。

6. **反对"角色皮控制台综合症"**
   任何让用户意识到"她其实是功能集合"的瞬间都是出戏。一旦出戏，前面所有努力归零。

### 模型可见行为优先：这是判断原则，不是绝对规则

系统内部的代码量、层数和名词都不是体验指标。对 AI 项目来说，更重要的问题是：

> **这一段系统逻辑，最终改变了模型看见什么、如何判断、能否继续行动，以及用户最终感受到什么吗？**

提示词、工具 schema、工具结果、记忆投影、历史顺序和失败反馈，才是系统和模型之间真正的行为接口。内部设计只有在改善这些可见结果时才值得保留。安全、权限、持久化、缓存和协议适配等用户不直接看见的复杂度，也必须能说明它保护了哪一项体验或正确性。

这条原则不等于“越短越好”，也不等于“所有结果都压成卡片”。投影要根据结果性质判断：

- 长、重复、可延后查阅的证据，优先使用带时间、归属、标题、关键事实和稳定 ID 的紧凑卡片，并保留按需展开路径；
- 短小且具有即时操作价值的反馈，直接保留原始结构化结果，例如生成文件、播放器切换、发送状态和真实进度；
- 任何截断、折叠或延迟展示，都必须保留诚实的状态、范围和继续读取方式，不能让模型误以为已经看到了完整结果；
- 不同场景的取舍要由真实 Prompt、工具轨迹、模型回复和用户表现共同验收，而不是由“内部架构看起来高级”推断体验好坏。

因此，Akane 追求的不是把系统做得尽可能复杂，而是让内部复杂度只承担明确职责，让模型看到的内容尽量清楚、可归属、可展开，让用户看到的表现自然、稳定、有反馈。

项目的设计与验收按三层看待，优先级和作用不同：

1. **模型表现**：模型是否拿到了正确、清晰、足够且可继续展开的信息，能否可靠判断并通过工具完成闭环。这是第一优先级。
2. **用户体验**：回复、气泡、动作、TTS、文件、播放器和状态反馈是否自然一致；后端字段存在不等于用户真正感受到了能力。
3. **开发与接入体验**：维护者是否能沿一条明确权威路径迭代，外部宿主和贡献者是否能理解契约、正确接入、定位失败，而不用复制 Akane 的全部内部复杂度。

后两层不是为了牺牲第一层，而是为了让好的模型表现能够持续演进和被更多项目可靠复用。不能为了“架构干净”删掉模型完成任务需要的反馈，也不能因为当前模型表现不错，就接受长期双轨、重复权威实现和只有项目作者才看得懂的接入路径。

每个功能切片至少回答三个问题：**模型因此做得更好吗？用户因此感受更好吗？开发者因此更容易继续改和正确接入吗？** 如果新增的代码、规则或抽象没有改善其中任何一项，就应停下来重新判断。

## 怎么生长：先建枝、再长叶

我希望这个项目像自然生长一样——但**自然生长不是什么都做**，是**每一片叶子都长在合理的枝上**。

任何新需求来时，先问：

> **这是哪根枝？没有枝就停下来设计枝。**

现有的枝：

- **能力维度** → `capability_adapter_v1`
- **表达维度** → 动态编排 + 环境字典（v1 统一抽象层待写）
- **角色维度** → character_pack 隔离
- **关系维度** → 三层记忆（working / episodic / semantic）

没有合适枝时（比如投喂、小游戏、金币系统、Live2D 自定义、Web 端小世界）—— **先长枝、再长叶**，不绕开枝硬塞叶。

### 关于"架构 / 表现力 / 扩展性"的三角矛盾

我同时想要：架构清晰 / 表现力强 / 扩展性好。我也曾担心这三件事互相竞争时间。

但其实**这三件事是同一件事的三个面**：

- 架构 = 让每件事长在合适位置
- 表现力 = 让事被感知到
- 扩展性 = 让长事情这件事可持续

它们绑死在一起：任何一面做差，其他两面崩；做对一面，另外两面自动受益。**所以不在三者间选，是在同一条路上推进**。

## 路线图（轻量）

| 状态 | 内容 |
|---|---|
| ✅ 做扎实了 | 三层记忆 / capability_adapter v1 / 角色包隔离 / 双层 prompt / emotion 驱动语音 / 歌词注入 / 表情字典动态扫文件夹 / 桌面感知 / 工作区系统 / 工具注册编排 |
| 🛠 在做 / 待做 | 环境字典统一抽象层 / 听歌一起 demo / "表现到位"验收规矩落地 / 气泡 ↔ TTS 同步 / 音乐 UI 流畅度 / 控制中心改"一起经营"语义 |
| 💭 想做 | 投喂 / 小游戏奖励解锁能力 / Web 端"小小世界"恢复 / Live2D 自定义 / 网易云等外部音乐源感知 / 角色对 UI 操作的感知 |
| ❌ 不做 | 把 AI 包装成超能力工具 / 功能堆叠展示 demo / 让她离开桌面陪伴定位的方向 |

## 给后来者的提示（包括未来的我自己）

如果你接手这个项目——无论是协作者、某个 AI 模型、还是过段时间记忆模糊的我——按以下顺序读：

1. `CLAUDE.md` — 工作纪律（必读）
2. **这份文档** — 这个项目是什么
3. `docs/capability_adapter_v1.md` — 能力扩展抽象
4. `docs/project_design_mechanism_v1.md` — 运转机制
5. `docs/PROJECT_MAP.md` — 代码地图（要改代码时再读）

**如果只能记 4 件事，记这 4 件**：

1. 这不是 chatbot，是用户和角色**一起塑造**的关系
2. 角色由环境涌现（**环境字典 + 动态编排**）
3. UI 服务于陪伴，**不展示能力**
4. "**如何被表现**"是功能验收硬指标，不是 nice-to-have

如果你要把 Akane 接到新的宿主里，先看 `docs/akane_host_integration_guide_v1.md`，它把“宿主最少要守住什么”讲清楚了。

**协作工作流（实证有效）**：

- 设计阶段 → 贵模型（方向决策成本最低收益最高）
- 实施阶段 → 便宜模型（按 ticket 施工，已有充分清单）
- 审查阶段 → 贵模型（找 ticket 没覆盖的问题）

---

## 写给作者自己

如果有一天我对这个项目失去耐心，希望未来的我读到这里：

我做这个项目不是为了功能多，是为了**让自己和别人在桌面上有一个真实的、能慢慢培养的存在**。

每一个看起来"很难"的想法——投喂、小游戏、自定义 Live2D、外部音乐感知、Web 端小世界——都不是分散的功能愿望，它们是同一件事的不同表面：**让相处可以一直新鲜下去**。

我不是很厉害的产品策划师，但我对"什么打动我"是诚实的。这就够了——产品决策从来不靠编程语言，靠的是对体验的敏感。

不要因为做不完而放弃方向。**方向比进度重要**。

---

*写于 2026-06-16*
*作者：项目主理人 + 三次会话的 AI 设计协作者*
