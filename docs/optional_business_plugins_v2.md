# 剩余可选业务能力插件化 V2

日期：2026-09-06。起点：`87c1c6f`。状态：分轨、人声净化、文件转写、素材集、生图、翻唱与文档七项均已切换；旧业务入口已删除或收敛为已验证的薄绑定。

目标是持续完成七项已识别能力，不以一个插件完成代替整项目标完成。已完成的媒体转换及其资源、产物、市场、Job 基础见 [V1 验收](optional_media_plugin_migration_v1.md)。每项验证、清除旧实现并聚焦提交后继续下一项。

## 审计结论与顺序

下表的“边界”是实现前审计结论，不代表能力已发布。最终包数量由共享依赖与生命周期决定，不强制一工具一包。

| 顺序 | 能力 | 当前权威与调用者 | 迁移边界 / 必须保留的行为 | 状态 |
|---|---|---|---|---|
| 1 | 分轨 | 原内置函数/handler/spec 已删除；插件为模型入口，脚本为包的薄绑定 | Demucs CPU/CUDA 与媒体服务 Demucs/UVR；两件产物；wav/flac/mp3；真实 CPU 已验收，GPU/UVR 真机未验收 | 已切换，见分轨 C |
| 2 | 人声净化 | 原内置函数/handler/spec/filter 已删除；插件拥有唯一净化实现，素材集经公共插件依赖端口调用 | FFmpeg 基础处理、DeepFilterNet AI、auto 降级及真实后端说明；四模式、post_filter、三种格式；CPU 已验收 | 已切换，见净化 B/C |
| 3 | 文件转写 | 原文件工具及业务 helper 已删除；插件为文件入口，实时语音/媒体服务薄绑定唯一识别库 | 多输入、部分失败、合并/独立输出、md/txt/srt/vtt/json、时间戳、语言/VAD/模型选项；实时语音产品入口保留 | 已切换，见转写 B/C |
| 4 | 训练素材准备 | 原内置入口/预设/PCM/切片/helper/handler/spec 已删除；插件拥有整理配方 | 三预设、采样/双声道/静音选项、真实 WAV/ZIP/manifest、来源与质量统计；clean_first 经已启用净化插件；不训练模型 | 已切换，见素材集 B/C |
| 5 | 生图 | 旧 provider/service/handler/spec/固定提示已删除；插件是唯一业务入口，宿主保留模型服务配置 | 生成及参考图编辑、多图、尺寸/质量/格式；真实单图生成与编辑已验收，多图/蒙版/上传回退仅协议测试 | 已切换，见生图 B/C |
| 6 | 翻唱 | 旧服务/handler/spec/固定提示已删除；插件入口，独立库唯一协议、缓存与混音权威；本机服务薄绑定 | WebUI 与本机服务、模型选择、分轨/推理/立体声混音、缓存与 QQ/file/both 交付；真实 UVR、Demucs/RVC 与安装 Job 已验收 | 已切换，见翻唱 C |
| 7 | 文档生成 | 旧 compose/revise/style、渲染器、schema、handler 和固定提示已删除；公共 SDK 插件及独立库是唯一写入业务权威 | 11 格式、完整来源导出、真实版本、已有 Word/Excel 非破坏样式；实际安装生成与 Word/PDF/Excel 页面核对通过 | 已切换，见文档 C |

审计起点市场仅有媒体转换。最终 `plugins/market.toml` 有转换及本目标七项，共八个条目；未安装/禁用时不向模型暴露业务能力，不以宿主导入业务包作为替代入口。验收安装均使用隔离实例，未改变用户插件安装配置。

## 宿主与插件职责

- 宿主保留附件/生成文件句柄、会话隔离、公共输入资源端口、文件登记/发送、权限、执行协调、Job、MemCore 和渠道/桌面交付。
- 插件拥有可选业务参数、模型/外部服务调用、文档渲染、产物内容与业务健康检查。只导入公开 SDK，不回调宿主私有旧业务服务。
- 复用现有静态市场、wheel 暂存、权限确认、generation 和 last-good 生命周期；不新增安装器、任务队列或完成事件总线。
- 业务间确有共享实现时必须只有一个权威。不得为了“各自独立”复制 Demucs、RVC、文档渲染或来源解析。
- 不把插件名或 future-only 状态硬编码进稳定模型提示。能力与可用选项来自实际安装描述，native/兼容投影同源。
- 普通 FFmpeg 降噪不是专用去回声/去混响模型；保留原模式参数时也必须如实描述能力限制，不承诺恢复录音或完全移除混响。

## 第一组必须处理的跨边界调用

1. `GeneratedFileService.audio_separation_status()` 目前优先本机媒体服务，随后查 Demucs 包/命令。旧的“包存在即 ready”不满足新健康验收。
2. `scripts/akane_demucs_worker.py` 与 `scripts/akane_local_capability_host.py` 直接导入宿主 `separate_audio_with_demucs`。不能只删工具 handler 而遗漏这些执行入口。业务函数迁入唯一插件/可复用业务包后，这些脚本只能保留薄绑定；依赖未安装时返回明确不可用。
3. 本机媒体服务既提供分轨，也提供文件/实时 ASR 与 RVC。删除独立分轨宿主入口不意味着可删除整个 `LocalMediaExecutorClient` 或关闭该服务。
4. 旧媒体执行服务采用同步 HTTP 请求；断开请求不证明服务端推理已取消。新桥接必须等待真实终态或使用服务确有支持的取消确认，不能把客户端取消冒充远端进程退出。此项必须在替换前解决并测试，不绕过既有 Job。
5. 净化的 `auto` 可以在 AI 失败后回落基础处理，但结果必须带实际后端与降级原因；明确 `ai` 失败时不得输出基础结果并宣称 AI 成功。

## 依赖交付

市场 wheel 不是 GPU 环境安装器。每个条目必须写清运行时、模型/权重、系统二进制或外部服务如何提供，并以实际探测结果决定可用性。

- 可执行程序/独立 Python 环境用管理员配置的真实入口；不把依赖悄悄安装进宿主全局 Python，不假定 host site-packages 等于插件已交付依赖。
- FFmpeg/FFprobe 路径与前一插件保持一致的配置语义。重型运行时独立探测，不污染轻量插件 worker 的导入环境。
- 启动健康检查不能隐式下载大模型、调用付费生成接口或把 provider 密钥输出。缺依赖、版本不兼容、模型缺失应区分为稳定 reason。
- 模型配置/密钥继续遵守既有宿主保密边界。生图迁移前须审计 `model_service_config.py`、`runtime_settings.py` 与路由/UI 的现有设置链，不能留下新旧两套配置权威，也不能擅自删除用户已配置数据。
- 外部服务及权重需要新授权时记录具体阻塞；仍有同目标内可做工作则继续，不以 mock 替代业务完成。

### 2026-09-06 本机只读探测

| 探测 | 真实结果 | 能证明什么 |
|---|---|---|
| `importlib.util.find_spec` | torch、torchaudio、demucs、df、faster_whisper、reportlab、docx、openpyxl 均存在 | 仅模块定位，不证明可执行 |
| `shutil.which` | FFmpeg、FFprobe、Demucs、deepFilter 存在 | 仅入口定位 |
| 导入 torch 与 Demucs | 成功；torch `2.12.0+cpu`，当前解释器 CUDA 不可用 | CPU 导入可用，不代表所有独立环境均无 GPU |
| 禁止下载时 `get_model('htdemucs')` | 成功加载现有缓存；44100 Hz，drums/bass/other/vocals | 可使用现有权重开展真实 CPU 推理验收；尚未证明音频分离质量 |
| `deepFilter --help` | 失败：`ModuleNotFoundError: torchaudio.backend` | 当前默认 DeepFilterNet 环境不兼容，不能标记 AI ready；未修改环境 |

本轮没有读出密钥、安装依赖、下载模型、修改用户实例或发起付费请求。其他模型、RVC 服务与生图 provider 尚未进行真实业务验收。

## 单项完成门槛

每个能力组使用相同门槛，并补充其业务信号/内容检验：

1. **来源与删除清单**：`rg` 列出内置实现、所有调用者、schema、提示/Skill、配置、状态/UI 和测试。明确最终 `deleted / thin adapter` 状态。
2. **依赖与安装**：从源码构建真实 wheel，经市场 hash/大小校验、暂存、审批、隔离激活；缺依赖/坏候选不替换 last-good；声明与实际依赖一致。
3. **发现**：未安装不可见；安装/启用可见；停用/卸载不可见；native 与兼容投影来自同一 descriptor；普通调用前后稳定 schema/hash 不变。
4. **实际业务**：附件和生成文件两种输入，原件不变；真实第三方推理/FFmpeg/provider/render 输出，检验内容而非文件存在；保留旧选项和真实 fallback，不缩成容易通过的子集。
5. **资源与安全**：跨会话、未知/过期句柄、越权、路径逃逸、坏媒体/文档、超预算、部分失败；公开结果无内部缓存/数据库路径和密钥。
6. **Job 与取消**：真实长调用前台返回 Job；重复触发幂等；取消等待实际清理、无孤儿进程、无迟到登记或重复完成；抑制取消的真实完成不能误报 cancelled；错误不能误报 succeeded。
7. **体验**：每件真实产物进入已有工作区、QQ/桌面交付边界；不默认重复发送/播放；失败有可见状态，气泡/TTS/音乐状态不矛盾。文本、音频、图片、文档各按实际媒介检查。
8. **生命周期**：运行中停用/卸载、再启用、依赖失败与候选失败；工具、worker、资源与任务状态一致，不留未来占位提示。
9. **唯一权威**：删除旧业务函数、注册、配置及提示；共享调用改为薄绑定。`rg` 与行为测试证明没有第二套入口继续拥有业务。
10. **提交**：相关 build/test/smoke、`ruff`、`git diff --check`；审查 status/stat/cached 清单，只提交本项文件，然后继续下一项。

业务专项验收：分轨两轨可解码、长度/声道符合输入与模型，并有真实推理证据；净化检查信号变化与实际后端；转写检查真实语音文本/字幕时间；素材集核验 ZIP/manifest/切片一致性；生图检查实际生成/编辑图片；翻唱检查实际模型输出与混音；文档打开/渲染检查内容和版式。

## 迁移窗口与状态记录

审计起点 `87c1c6f`：七项仍由原内置实现提供，尚无第二条已发布业务路径。窗口内不向模型同时发布同一业务的新旧工具。每项替换必须记录新权威、保留旧逻辑的原因、待删除具体符号、约束测试及关闭提交；不能长期保留迁移窗口。

审计切片仅新增本文档（`276f2f7`）。DeepFilterNet 环境问题留作明确风险，不在宿主里伪造兼容模块掩盖错误。

审计切片验证：`git diff --check` 通过。`python -m unittest tests.test_package_reintegration_policy -q` 共 12 项，11 通过、1 失败：`test_dynamic_adapter_execution_uses_single_capcore_prepare_gate` 禁止源码出现 `capcore_build_permission_request`，但基线 `87c1c6f` 的 adapter 已有该导入。已用 `git show` 核对基线导入与断言，两个文件相对基线均无修改。本切片不把这组标为全绿，也不扩大到权限策略修复。

### 分轨 A：独立离线工作进程

- `plugins/akane_audio_separation` 中的业务工作进程不导入宿主私有实现。显式 ML Python 负责加载已存在、校验通过的 Demucs 模型；不使用下载兜底。
- 输入为父进程预处理后的 PCM WAV，工作进程不启动 FFmpeg 子进程或多进程推理池；真实模型生成两条独立音轨，保留 CPU/CUDA 选择与 CUDA 失败后的 CPU 重试。
- 进程生命周期覆盖启动中的取消、重复取消、超时、关闭时仍在启动的子进程；等待实际退出才确认取消。此处不引入另一条宿主任务队列。
- 已运行真实 CPU 分轨：两秒双声道混合信号输出完整双声道两轨、长度不变、不同于输入和彼此，原输入哈希不变。120 秒输入的实际 ML 工作进程在取消后退出，未生成伪成功音轨。这是模型/信号与进程验收，不代表真实歌曲的主观听感已验收。
- 临时迁移窗口从 `276f2f7` 开始：A 完成时新包尚无插件注册或市场条目；原内置仍是唯一产品入口。接入资源/descriptor、远端 Demucs/UVR、市场与双产物交付并验收后，删除 `generated_files_media.separate_audio_stems` / `separate_audio_with_demucs`、服务专属 helper/handler/spec，将两个本机脚本改为薄绑定并关闭窗口。
- 约束命令：`python -m unittest discover -s plugins/akane_audio_separation/tests -v`；最终入口删除仍须宿主安装/发现/生命周期和旧名称守卫测试，不能以本地工作进程测试代替整项迁移。
- 本切片最终 9 项测试全部通过（含真实 CPU 模型、坏权重校验和实际进程取消，无跳过）；新包 `ruff check`、格式检查与 `git diff --check` 通过。未安装插件、未修改宿主运行时或全局依赖。

### 分轨 B：SDK、远端协议和真实隔离安装

- 新包增加真实 SDK entry point 与 descriptor，使用当前调用资源端口；真实 Demucs 两轨经 FFmpeg/FFprobe 验证、三种格式编码后返回有序多产物。生成文件可再作为输入；默认只登记，不自动重复发送。
- 远端客户端复用 `/health`、`/v1/audio/separate`、`/v1/rvc/separate` 同步协议，标准库分块上传、下载和 ZIP 解包，不把大文件装进 JSON 或整块内存。匹配格式的远端产物直接移动，不重复有损编码。视频先提取音轨，普通压缩音频保持原始传输格式。
- 远端优先选择和本地 fallback 在每次调用重新检查。明确 remote 模式不擅自落到 local。远端协议测试使用真实本地 HTTP 服务和明确的 fixture 音轨，不冒充真实 UVR 推理。
- 旧服务没有停止推理协议。取消先等真实成功响应再丢弃产物并确认 cancelled；连接丢失或服务错误可能表示内部 RVC 仍在运行，返回 `remote_completion_unconfirmed`，不发起重试、不误报已停止。
- 隔离 generation 复用 `PluginHost` 已有参数，将健康检查预算设为 30 秒，仍处于父进程默认 45 秒启动预算内；普通进程内 PluginHost 默认不变。原因是实际离线加载模型不能可靠塞进原 5 秒检查窗口，不新增第二套激活状态机。
- `tests.test_audio_separation_plugin` 从复制源码构建真实 wheel 和私有静态市场目录，经暂存/权限确认/隔离激活进入真实 Engine 工具桥；验证 WAV/FLAC/MP3 双产物、跨会话拒绝、原件不变、临时副本清空、同源 native/兼容描述及 schema 稳定、坏模型候选保留旧代、停用/启用/卸载。
- 分轨、既有转换及 generation/candidate/runtime 组合回归 37 项通过；最终源码测试 16 项全部通过，更新后的真实 wheel 完整安装/业务/生命周期用例再次通过（63.991 秒）。最后 UVR 中文模型名与非法控制字符检查后的远端 7 项再通过；`ruff`、格式和 diff 检查通过。以上回归有重叠，不相加宣称独立用例总数。
- 用户市场索引尚不增加分轨条目，用户实例没有安装。下一切片必须处理旧配置/脚本绑定、完整 Job/渠道表现与旧实现删除，随后发布真实市场条目；不能把私有目录安装成功当成整个分轨迁移已经完成。

### 分轨 C：旧入口删除、共享调用收口与产品执行验收

- 删除宿主分轨业务函数、Demucs 推理/命令 fallback、专属状态/构造参数、handler/spec/固定提示、旧客户端分轨方法及专属卡片生成；历史生成文件仍可读取和发送，不迁移或删除用户文件。旧工具不会因附件出现而重新可见。
- `MediaTools` 与 `LocalDemucs.separate_media` 统一 FFmpeg 预处理和独立推理；本机服务/兼容 CLI 改为薄绑定，保留独立 Python/package-root/FFmpeg 配置以及外部运行时不可用时的本机 fallback。真实模型加载决定 ready，不再使用 find_spec 假定可执行。
- 歌词时间轴的隐藏 Demucs 调用/命令路径已删除，改为调用实际安装启用的分轨能力，复用普通权限 admission 和 ExecutorBroker。未安装、停用或未获许可时回退 mixed ASR，不自动安装或批准；人声文件直接转写、伴奏跳过的行为保留。ASR 临时文件只写到调用工作目录，原件旁边不再留下 prepared 文件。内部产物通过普通文件登记存在，不宣称仅是私有缓存。
- 插件配置属于可选业务，原 `LOCAL_MEDIA_EXECUTOR_BASE_URL`、RVC 分离模型配置仍供 ASR/翻唱使用，不删除共享服务。原远端用户须把现有端点显式配置为进程环境 `AKANE_SEPARATION_REMOTE_URL`；不私下导入宿主 `.env`、不修改实例环境、不假定设置会自动传给 worker。市场条目与 README 写明迁移要求。
- 从真实双条目市场构建 wheel、校验暂存/审批/隔离安装；默认不启用用户实例。桌宠/QQ 模式 prompt 与 native/兼容描述只随实际安装启停变化，普通调用不改变稳定 schema。源码市场已构建到忽略目录，未公网发布。
- 真实长 Job 在前台迅速返回，成功登记两轨；同一调用不重复创建，跨用户不能取消；120 秒源音频真实预处理/推理后重复取消，无迟到登记。运行中停用先撤掉发现入口，再等待原调用取消清理，之后可重新启用。未知句柄得到 failed，不伪造成功。
- 两件真实 MP3 产物分别经过既有 MemCore 终态幂等记录、send_file、桌面交付模块 smoke 与 OneBot 文件上传边界验证；默认 available-not-delivered，不自动播放两轨。OneBot 传输响应是测试替身，不是实际 QQ 发送；未声称桌宠真人点击/试听或 GPU 主观质量验收。
- 真实 CPU 验证覆盖本地插件 WAV/FLAC/MP3、独立 CLI、同步服务绑定和 ASGI `/v1/audio/separate` 的实际 ZIP 两轨；两秒输入长度/声道正确、音轨互不相同、原件不变。远端客户端 Demucs/UVR、异常 ZIP、取消确认等使用真实 HTTP fixture；没有配置现成远端服务（只读配置探测 `configured=false`），不把这些 fixture 算作真实 UVR/GPU 推理。
- 窗口状态：从 `276f2f7` 开始的旧分轨产品入口已关闭。`tests.test_optional_media_binding` 守卫宿主/脚本不再含旧入口、普通权限不被内部调用绕过、缺插件不执行 ML。仅内部歌词消费者保留稳定能力 ID 绑定，不把它加入稳定模型提示。
- 已通过：旧入口删除相关组合回归 218 项；路由、generation/candidate/runtime、插件 Engine 桥、Host Job 组合 163 项；真实市场分轨/转换及市场协议组合 11 项；服务/CLI/真实 ASGI 分轨 5 项。各组有重叠，不相加当作独立总数。
- 最终包内 19 项全部通过（55.830 秒，无跳过），含视频画面长于音轨、全流程超时、实际进程取消与坏 package-root。最新真实市场完整分轨用例再次通过（107.700 秒），含双模式发现/schema 稳定、Job 和运行中停用；时间轴/权限/原件保护聚焦 8 项通过。新增文件及包格式检查、相关 `ruff check`、Python 编译、`git diff --check` 均通过。
- 桌面活动整组 28 项中 27 通过，1 项旧提示断言失败：`test_desktop_audio_capability_discourages_task_workspace_for_playback_control` 要求旧句子“不要为这些动作创建任务工作区或委派后台任务”。已核对该测试方法与 HEAD 完全一致，HEAD 宿主源码也不含此句；本次不补回无关提示来使断言变绿。时间轴相关行为与新权限/原件保护测试通过。此前记录的 MCP/策略基线失败也未混入修复。

后续仍按顺序推进净化、文件转写、素材、生图、翻唱、文档；CPU 分轨代码切换不代表其余六项或 GPU/UVR 真机边界已经完成。

### 净化前 repair：共享子进程生命周期

- 净化审计发现转换插件在子进程创建过程中取消时可能遗留进程。先暂停新业务接入，
  把分轨已经验证的启动/运行/超时/关闭回收逻辑移到公开 SDK `plugin_subprocess`。
  转换直接使用此实现，分轨 `process` 只保留兼容重导出；不新增业务规则或任务队列。
- 转换新增真实子进程启动期间重复取消测试，以及超时的结构化错误/半成品清理测试。
  独立 ML 工作进程不依赖 SDK，父进程需要当前 release 的公共辅助模块。
- 人声净化仍由原入口提供，本 repair 不把尚未接入的插件暴露给模型。
- 验证通过：转换源码 12 项、分轨源码 19 项（含真实 CPU 推理，59.528 秒）、
  服务/CLI 5 项，以及真实市场安装/业务/Job/生命周期组合 11 项（141.894 秒）。
  首轮发现远端路径漏重导出 `drain` 并已修复，随后上述完整组合通过。
  市场构建、相关 `ruff`/格式与 `git diff --check` 通过；没有安装到用户实例。

### 净化 A：业务运行时与诚实降级

- 从 `1fe951d` 开始暂存迁移窗口：`plugins/akane_voice_clean` 目前仅为独立业务库，
  没有 entry point 或市场条目；原内置仍是唯一产品入口，宿主不调用新库。
- 新运行时保留四模式、后置滤波、WAV/FLAC/MP3 与 auto/basic/ai。基础滤波真实作用于
  信号；去混响/去回声只是降噪预设的限制明确进入结果。AI 子进程只读取已准备的模型，
  禁止隐式下载，不使用 DF CLI 的 DataLoader 子进程；模型加载和短推理决定 AI ready。
- 当前默认环境的实际调用返回 `deepfilternet_runtime_incompatible`，auto 真实回退基础，
  显式 ai 不产出冒充 AI 的结果；默认 DeepFilterNet 缓存目录的只读检查也显示不存在。
  已询问用户可用独立环境/模型，或另建隔离环境和下载权重的授权，尚未进行依赖部署。
- 源码 13 项中 12 通过、1 因缺可用 AI 环境跳过（18.152 秒）。覆盖所有格式/模式/后置滤波、
  真实信号低频抑制/主信号保留、原件保护、实际默认环境降级、缺模型不下载、视频音轨时长、
  FFmpeg 运行中重复取消及实际超时清理。`ruff`、格式和 diff 检查通过；不是 AI 质量验收。
- 下一步关闭窗口：接入资源端口、单件产物、真实 SDK/市场/Job/渠道/生命周期验证，然后删除
  `generated_files_media.clean_voice_track`、`GeneratedFileService` 净化状态和所有专属 helper、
  handler/spec/固定提示；素材集 `clean_first` 改为已安装净化能力的普通权限调用，不保留另一套
  滤波配方。约束命令：包内 unittest；最终旧名称守卫和真实安装测试必须一并通过。

### 净化 A2：非系统盘隔离环境与真实 AI 验收

- 用户已明确允许下载，并要求优先使用空间充足的非 C 盘。只读检查 F 盘剩余约 469 GB、
  C 盘约 11 GB；隔离 venv、包缓存、模型原始 ZIP/解压权重及本轮 TEMP/TMP 全部放在 F 盘
  的源码仓库外。未修改宿主全局 Python、用户实例设置或环境变量持久配置，未启动/重启实例。
- 已部署 CPU 组合：Python 3.11、torch/torchaudio `2.6.0+cpu`、DeepFilterNet `0.5.6`、
  NumPy `1.26.4`、SoundFile `0.12.1`；`uv pip check` 26 个包兼容。宿主原 Torch 仍为
  `2.12.0+cpu`。CPU wheel 来自 PyTorch 官方索引，其余来自 PyPI，权重来自 DeepFilterNet
  官方仓库；ZIP 哈希和可复现配置已写入包 README，机器运行时目录另有说明和版本清单。
- 真实权重加载、短推理与完整 AI 音频净化通过。新增真实后置滤波信号变化、120 秒源音频
  AI 工作进程重复取消、空 checkpoint 拒绝、禁止派生子进程时仍可推理的回归。
- 验收发现 DF 日志初始化会启动 Git 元数据查询，已改用库支持的 `log_level="none"`。
  Python 3.11 Windows 平台查询也可能启动 `cmd /c ver`；worker 审计钩子拒绝子进程后，
  标准库使用真实系统 API 回退。没有伪造模块/平台值，不扩展另一套 Job 或进程树管理器。
- 指定 F 盘环境后源码 17 项全部通过（40.400 秒，无跳过），实际 CPU 模型与子进程取消均
  已执行；`ruff`、格式、diff 检查通过。本轮临时目录最终为空，模型/安装缓存保留供复用。
- 此项仅关闭“缺可用 AI 环境”的验收缺口。仍没有净化 entry point/市场条目，旧入口删除、
  资源/Job/渠道和生命周期接入尚未完成；不把依赖安装或信号测试当成整个净化迁移完成。

### 净化 B/C：唯一插件入口与实际产品链路

- 关闭从 `1fe951d` 开始的净化迁移窗口。删除旧内置工具、状态、参数归一化、
  DeepFilter CLI/输出查找、滤镜、专属卡片生成和固定模型提示；历史产物仍能读取交付。
  公共 SDK entry point/descriptor 与静态市场条目只对应现有实际实现。
- 单件输出通过当前会话资源端口、托管产物接口；真实 wheel 安装/暂存/权限确认/
  隔离 generation 验证后，QQ/桌面 native 与兼容描述同源且调用前后稳定。
  停用/卸载后不可见，FFmpeg 缺失的候选不会替换正在运行的代。
- 素材集 `clean_first` 经普通权限入口与 ExecutorBroker 调用已安装净化能力，
  保留先前 basic/voice_focus 规则与实际渠道。缺插件/停用/需要批准会令该来源失败，
  不继续生成“已净化”的切片。成功调用登记的净化音频是普通产物，不伪装私有缓存。
  完整链路核对 ZIP/manifest/切片；缺插件时没有成功产物。
- 三种格式均执行真实 CPU DeepFilterNet 推理、48kHz 单声道、长度正确、原件哈希不变。
  模型反馈明确实际 AI 或基础后端、auto 降级原因与原件未修改；显式 ai 缺依赖直接失败。
  四模式/后置滤波/信号变化/坏模型/全程取消由包内真实测试覆盖。
- 真实 Host Job 前台迅速返回，重复提交幂等、跨用户不能取消；120 秒输入完成预处理并
  进入模型进程后重复取消，运行中停用撤掉发现入口，清理结束后无迟到产物、可再次启用。
  成功/失败/取消终态经 MemCore 幂等记录；实际 MP3 经过 send_file、桌面交付模块 smoke
  和 QQ 文件上传边界。QQ 响应仍为测试替身，未实际网络投递、未冒充真人试听/GPU 验收。
- 已通过：旧入口/发现/来源/权限组合 214 项；路由/generation/Engine/Job 组合 163 项；
  包内 17 项（45.502 秒，无跳过，实际 CPU）。各组有重叠，不累加成独立用例数。
  净化/转换真实市场安装与市场协议最终 11 项通过（89.355 秒），包含完整 AI/Job/
  素材集/auto 降级场景。扩展市场后转换测试改为按 ID 选条目，不依赖首条位置。
  市场构建、相关 ruff/格式/编译及 diff 检查通过。
- 仅构建仓库忽略目录内的市场索引；未替用户实例启用插件、未公网发布、未更改全局
  Python 或持久化环境设置。F 盘独立环境配置方式保留在包 README。

### 文件转写 A：离线推理、格式与远端生命周期

- 新建独立业务库 `plugins/akane_file_transcription`，当前还没有 entry point/市场条目；
  旧文件转写入口仍是产品权威，临时窗口从净化完成提交 `a9a31b6` 起。
  切换时需删除旧模型/预处理/转写/渲染/handler/spec，歌词时间轴与本机服务改薄绑定。
- 本机缓存的 Whisper tiny 已实际离线加载，无新下载。真实 Windows Zira 语音合成
  录音经 faster-whisper 识别，核对 hello world、speech recognition、字幕时间范围、
  VAD 开关、视频音轨时长、静音/坏媒体拒绝、长语音重复取消后的实际进程退出。
- 新进程仅读取已预处理 PCM，模型/cache/device/compute/language/VAD 都走真实调用。
  cache 查找 local-only，并要求 tokenizer 文件存在以堵住 faster-whisper 自带在线
  tokenizer fallback；离线变量只设置在该 ML 子进程，不修改宿主环境。
- md/txt/json/srt/vtt 的单份及合并渲染由新库拥有。远端使用现有同步 ASR 协议，
  分块上传、限制结果体，取消等待真实响应；连接丢失不冒充服务端已停止。
  HTTP 测试使用明确 fixture 文本，不计作真实远端语音识别。实时语音产品入口不迁移。
- 本切片 12 项全部通过（48.908 秒，无跳过），包括实际 CPU 推理/取消与真实本地 HTTP
  传输；新包 ruff、格式和 diff 检查通过。尚不能把这组业务测试称为完成插件迁移。

### 转写 B/C：唯一识别实现、共享入口与真实字幕交付

- 关闭从 `a9a31b6` 开始的文件转写迁移窗口。删除旧文件转写函数、批量来源解析、
  模型/预处理/识别实现、五格式渲染、handler/spec/固定提示与专属卡片；历史稿件仍可读取。
  新 SDK descriptor/entry point 和市场条目只在显式安装启用后可见。
- `inference.py` 唯一拥有离线模型加载、PCM 读取和识别；实时语音原产品入口不迁移，
  原六个 helper 仅薄绑定公开 compatibility API。本机媒体服务也只绑定 LocalTranscriber，
  保留同步完成响应。源代码 release 须附带该共享业务库，缺库结构化失败；源代码库解析
  不是文件插件自动启用，也不改变宿主实时语音配置。
- 歌词时间轴只向实际安装工具传当前会话句柄，经普通权限入口执行；没有 ASR 插件则
  报不可用，不暗中加载 ML。可选分轨不可用仍用混合音频，伴奏跳过、人声直转保留。
  JSON 稿按实际来源句柄复用，不把多来源全文错用为任意歌曲字幕。
- 实际市场 wheel 校验、暂存、权限确认和隔离激活后完成真实 tiny CPU 识别；附件和
  生成音频交替输入，五格式均核对 hello world 与字幕时间，批量部分失败如实报告，
  合并/独立产物和来源隔离均验收。Windows 系统 .srt 关联可能返回 text/plain，公共
  产物校验现明确允许规范 application/x-subrip，VTT 同理；没有关闭 MIME 校验。
- 真实 Job 快速返回且重复提交幂等；长语音完成预处理进入推理后重复取消、运行中停用，
  实际进程退出、没有迟到产物、再启用正常。缺缓存的坏候选保留 last-good；卸载撤掉
  native/兼容发现。成功/失败/取消终态经 MemCore 幂等，实际 JSON 经既有 send_file、
  桌面交付模块 smoke 和 OneBot 上传边界；QQ 响应是测试替身，非实际网络投递。
- 最终真实市场完整用例通过（242.023 秒）；字幕 MIME 与托管产物 16 项通过。
  切换后的包内 12 项再次全部通过（62.590 秒，无跳过，含真实推理/取消）。
  旧入口/工具/schema/生成文件组合 233 项通过，路由/generation/Engine/Host Job 163 项
  通过；共享实时语音与本机媒体服务 8 项通过（含真实 ASGI 识别和现有真实 Demucs），
  实时语音不受无效文件插件配置影响的额外用例通过。各组有重叠，不累加为独立总数。
- 桌面/语音/Skill 组合 55 项中 54 通过；唯一失败仍是前述旧播放提示文字断言，
  不属于本项变化，未为其恢复无关提示。未声称 GPU、多语言质量或真实远端 ASR 验收；
  未启用用户实例插件、未修改持久设置、未公网发布。

### 素材集 A：真实切片/质量清单与可取消归档

- 从 `d52416f` 开始临时迁移窗口。新 `plugins/akane_voice_dataset` 仍是独立业务库，
  无插件 entry point/市场条目；旧内置仍是唯一产品入口。下一步完成依赖调用、SDK、
  市场/Job/交付验收后删除旧 prepare_voice_dataset、预设/PCM/切片/manifest/helper、
  handler/spec/固定提示；不新增调用旧实现的入口。
- 新库通过 FFmpeg 预处理，标准库工作进程切片、分析并生成 WAV、ZIP 和来源关联 manifest。
  三种预设、静音阈值/保留时长、音量标准化、部分失败、质量标记保留。min/max 长度是
  质量标记，不误称硬切；全静音保留为 empty_or_failed，推荐数为零，不伪造有效语音。
- 修正旧参数与实物不符：省略值使用真正的预设采样率；mono=false 保留双声道，反相
  声道不会在检测时相消成假静音。显式非法值拒绝，不悄悄钳制。96 kHz 先传受控 raw PCM
  给工作进程，再写标准 PCM WAV，避免 Python 3.11 不支持的 FFmpeg 扩展 WAV 头。
- 所有子进程由公共 SDK 管理，工作进程不派生子进程。限制来源数/时长/PCM/ZIP/切片数；
  超限失败而不是偷偷截短。实际 CPU 归档与 FFmpeg 的重复取消、超时先回收后清理。
- clean_first 不含复制的净化配方，也不凭布尔参数假装已处理；后续插件须通过正常权限
  调用已启用净化能力。当前源码库对未实际净化来源返回 dataset_cleaning_required。
- 本切片包内 12 项全部通过（10.183 秒，无跳过），含真实 96 kHz、双声道、FFmpeg 和
  CPU 工作进程重复取消、真实超时、坏来源和原件哈希保护。ruff/格式及 diff 检查通过。

### 素材集前 repair：隔离进程的取消终态

- 接 clean_first 依赖调用时发现父进程把“已发出取消”一律当作 cancelled，可能盖掉
  worker 抑制取消后返回的真实成功，或 remote_completion_unconfirmed 失败。
  先暂停新插件接入，修复现有 generation 返回链，不建立另一套取消/Job 状态机。
- 父进程现在等 worker 真实终态；只有其确认 cancelled 才向上传播取消。实际成功完成
  正常产物交接，实际失败保留 reason；重复取消也等该终态的交接结束。
- 新真实隔离进程测试覆盖取消、抑制取消后成功（实际 TXT 登记）、完成未确认失败，
  并经普通插件工具桥、ExecutorBroker 和 Host Job 核验三种正确终态且完成事件只一次。
  这不是把 HTTP fixture 当作真实远端推理；这里只验证取消协议与终态传递。
- 组合 generation/candidate/runtime/Host Job 51 项通过（31.225 秒）；新增真实进程和
  Job 用例 2 项通过（5.205 秒）。ruff、格式与 diff 检查通过。

### 素材集前置：受权限约束的插件依赖调用

- 公共 SDK 增加显式 `capability.invoke` 权限与 invocation-local 端口；调用目标必须
  是实际启用的产物型插件，仍经原 CapCore 权限入口、工具桥和 ExecutorBroker。
  宿主只负责调度/会话边界，不含净化配方，不创建第二套 Job 或发送队列。
- 依赖调用只返回已登记产物句柄，禁止 send_to_user=true；身份继承宿主当前调用，
  参数不能替换 profile/session。真实隔离进程验收循环依赖、跨会话、预算、禁用、
  重新启用和 approval_required，未授权目标不会产生文件。
- 取消沿原 callback 通道传递并等待实际依赖退出；远端完成未确认仍是失败。
  子调用若实际完成，其既有产物保留；父调用取消不会再生成父产物，不假称前者被撤销。
  运行中移除目标先撤发现，再等待租约和真实进程回收。
- 调用/宿主组合 33 项通过（77.255 秒）；generation/candidate/runtime/Engine/Host
  71 项通过（77.515 秒）；资源/终态 8 项通过（20.499 秒）。包括真实独立 worker
  和实际文件转化，不把测试文本转换称为业务模型验收。ruff 与 diff 检查通过。

### 素材集 B/C：唯一整理实现与真实归档交付

- 关闭 `d52416f` 开始的迁移窗口。旧内置 prepare_voice_dataset、预设、PCM/切片/质量
  helper、ZIP/readme 构造、handler/spec、固定提示和宿主 prepare_dataset_voice 绑定删除。
  历史 ZIP 的通用读取/检查保留；新插件不导入旧私有实现。
- 显式安装 wheel 后提供一份 descriptor，native/兼容投影同源，调用前后稳定；未安装、
  停用、卸载均撤掉发现。clean_first 经公共权限/会话端口调用实际净化插件，基础处理
  成功才标记 cleaned；禁用/缺失净化没有 ZIP，不复制滤波配方。中间音频如实登记，
  默认不发送、不播放，并在用户结果说明中明确这一点。
- 实际三种预设、附件/生成来源、96 kHz 双声道反相音频、音量标准化、长度质量标记、
  部分失败均核对 ZIP 成员、WAV 参数、来源关联和 manifest；原件哈希不变。
  标准 ZIP MIME 不再受 Windows application/x-zip-compressed 注册表关联影响，仍保留
  类型校验；新增 portable MIME 回归覆盖该情况。
- 真实 10 分钟高采样率 PCM 归档 Job 快速提交，运行中停用撤发现，重复取消后实际进程
  回收、无迟到产物；再启用可调用，缺 FFmpeg 候选保留 last-good。成功/失败/取消
  三终态进入原 MemCore 幂等链；真实 ZIP 经原 send_file 和桌面交付模块 smoke。
  OneBot 仅验证真实文件上传边界、HTTP 使用替身，未作实际 QQ 投递。
- 完整市场验收单项通过（88.510 秒）；切换后市场/ZIP/路由组合 127 项通过（124.095 秒）。
  工具/schema/提示/旧调用链组合 202 项通过（32.172 秒）；包内真实处理 12 项通过
  （9.179 秒）；净化真实 DeepFilterNet/市场全链回归通过（75.670 秒，无跳过）。
  ruff、格式与 diff 检查通过。未改用户持久设置、未公网发布、未增加训练或标注功能。

### 生图前置：配置连接边界与安装权限 repair

- 原模型服务设置继续拥有生图配置；新增只在实际调用内可读的命名连接 SDK，权限为
  connection.image_generation.read。每次从当前 Bot 设置取生图专用 key（保留原聊天
  key 后备规则）、base/model 和必要预算，不复制配置文件、不返回其他视觉/聊天配置。
  设置重载和禁用在下一次调用生效。私有结果不进入公开 callback、prompt 或产物。
- 注册/健康/暂存没有 invocation，不能取密钥；安装只检查本地运行依赖，不把该检查
  称为远端生成成功。真实调用才检查提供商；暂存不发起付费生成请求。
- 审查发现已安装 worker 声明权限未与安装批准记录比对，先修复此处。候选进程必须
  与托管制品批准集合一致，否则拒绝发布；未携带安装审批记录的旧 process-installed
  路径不能取新连接。该机制仍是可信 Python 插件的逻辑边界，不宣称是操作系统沙箱。
- 真实隔离进程以本地 HTTP fixture 验证凭证投递、轮换、普通 approval_required、
  禁用、跨 Bot 隔离、启动阶段拒绝读取、回调伪造/过期拒绝和权限漂移；没有把 fixture
  当作真实生图。连接/资源/组合/终态 22 项通过（75.638 秒），安装/宿主组合 51 项通过
  （112.156 秒），generation/工具桥/模型设置组合 93 项通过（79.766 秒）。
  最后一轮安装/实际素材集市场回归 16 项通过（151.863 秒）。
- 用户已明确允许后续使用现有 PinAI 接口最多两次请求、最多三张新建测试图，限一次
  生成与一次参考图编辑；尚未执行这两次付费验收，不上传用户私有图片。

### 生图 A：独立协议库，尚未切换产品入口

- 从 `6fc43ec` 开始短迁移窗口。`plugins/akane_image_generation` 暂无 entry point/市场条目；
  原 image_generation provider/service 和 Engine 仍是唯一产品入口。B/C 验收后删除这些旧业务
  与旧 handler/spec/固定提示；通用 load_material 解析和现有模型服务配置保留。
- 新库包含生成、单/多参考图编辑、蒙版、四张输出预算、PNG/JPEG/WEBP、尺寸/质量/压缩；
  修复 compression=0 被改成 90、预览 SSE 被误当最终图、无界响应和只验魔数的问题。
  真正 Pillow 解码验证参考图/输出，PNG alpha 蒙版须匹配首参考图尺寸。
- HTTP 连接丢失不自动重试、不假称取消；只有明确的无兼容账户拒绝允许有界重试。
  多参考 multipart 被拒后兼容 file-ID 协议，并回收本次上传的 ID；不删除用户既有文件。
  达到请求张数的最终事件即可结束读取，不能等 relay 长连接；部分最终结果明确数量不足。
- 包内 11 项通过（2.687 秒），包括实际 HTTP 与编解码、流/字节边界、两种取消终态、
  格式/参数、蒙版与上传回退。仅 HTTP fixture，不算真实 PinAI 生图；付费额度尚未使用。
  迁移窗口验证：`python -m unittest discover -s plugins/akane_image_generation/tests -v`。

### 生图前置：无输入产物的临时目录

- resource port 增加 invocation 内工作目录，仍由原生命周期在产物交接、失败/取消排空后
  清理。用于没有参考图的生成请求，不要求伪造输入文件，也不新增持久存储或文件登记链。
- 真实 worker 验收 17 MiB 文件（超过内存 draft 限制）登记成功与运行中取消，临时目录和
  outbox 均回收；无调用/错误 owner/调用结束访问被拒。资源测试 8 项通过（15.102 秒）。
  宿主/产物/候选组合 46 项通过（6.792 秒）；ruff 与 diff 检查通过。

### 生图 B/C：市场安装、真实请求与旧实现退场

- `akane.image-generation.run.v1` 经公开 SDK 资源/命名连接/产物端口执行，使用既有 Job、
  MemCore、CapCore 审批和渠道交付。默认只登记不发送；中风险首次确认，未批准前不取密钥、
  不发请求。每次读取当前 Bot 设置，禁用及凭证轮换在下一次调用生效。
- 删除宿主 image_generation.py（provider/service）、GenerateImageToolHandler、Engine 工厂、
  内置注册、两份固定 schema、固定生图提示模块/目录标签和工具状态特判。
  load_material、附件/生成文件、现有模型服务设置仍为宿主公共基础，未修改红线 UI。
- 本地依赖需 requests/urllib3/Pillow 与支持工作目录的 SDK；三种真实图片编解码决定健康，
  不做 /models 或付费启动探测。插件可本地就绪但远端尚未验证，调用失败按实际原因返回。
- 三笔显式授权、无自动重试的真实 PinAI 请求：① n=2 生成被拒绝，未返回图片，未保存原始
  响应，不能断言具体拒绝原因；② n=1 编辑成功，新建蓝方块改成红方块，保留中心白圆与浅色
  背景，返回 1254×1254 PNG；③ 追加授权的 n=1 生成成功，蓝陶瓷杯/奶油背景，返回
  1402×1122 PNG。两次成功均请求 1024×1024，返回不同尺寸，已真实检查图片并如实提示。
  请求预算已用完，不重放；仅上传新建测试材料，没有使用用户私人图片。
- 本地 HTTP fixture 覆盖两张最终 SSE 图、生成句柄再编辑、附件/蒙版、JPEG compression=0、
  WEBP、跨会话拒绝、坏编解码候选保留 last-good、启停/卸载、Job 幂等及取消排空。
  断连后的远端终态不确定返回 failed，而不是 cancelled；无迟到产物/重复完成。
- 生成材料确实进入 load_material 私有像素链路；两个输出逐一通过原桌面文件消费代码和
  QQ image 消息组装/HTTP 替身验证。不声称真实 QQ 已发送、桌宠真机 UI 已点击或云端多图、
  mask、文件上传回退已实测。图片任务不自动播放音频，不修改 TTS/音乐状态。
- 验证：包内协议测试 13 项通过（2.702 秒）；真实市场安装链路 2 项通过（98.836 秒）；
  材料/工具发现/调用/执行/Job/唯一权威/命名连接/后端路由组合 221 项通过（55.618 秒）。
  `python scripts/build_plugin_market.py` 构建六项本地市场成功；新增库与测试 ruff 通过，
  `git diff --check` 通过。没有发布公网市场，也没有替用户实例安装/启用。
- 最终复核：执行/目录 40 项通过（7.472 秒）；媒体转换、generation 生命周期、插件工具桥
  16 项通过（56.243 秒）。首次组合命令误填不存在的测试模块名，修正为实际 engine bridge
  模块后重跑通过，不把该加载错误记为产品通过。

### 翻唱前置：真实运行环境与公共交付意图 repair

- 已审计旧 RVC WebUI / 本机媒体服务、双层缓存、模型选择、混音和语音/文件交付。
  本机既有独立 Python 3.9.13 / PyTorch CUDA 环境可运行，配置的默认音色与权重存在；
  原服务未启动。隔离副本仅监听回环端口，取消旧启动脚本的清目录行为，不修改原安装和缓存，
  复用原权重；未下载模型、安装依赖、启动公网分享或改用户实例配置。
- 一段新建离线测试语音完成真实 RVC 推理：40 kHz 单声道、5.98 秒 WAV；这是环境验收，
  尚不表示新插件或完整翻唱已经完成。原 UVR 请求也返回两条音轨，但实测
  `is_generating=true`，说明旧协议把生成器中间响应当终态；新协议必须完整排空，不能照搬。
- 公共产物增加 file/voice/both 交付意图，默认 file；只允许音频请求 voice/both。
  worker/outbox/引用校验/工具事件/Job 持久产物/Agent 完成事件贯通，保留旧 SDK 默认行为。
  完成事件明确“可用但未确认发送”，仍走现有 send_audio/send_file 或直接事件交付链，
  不新建发送队列，不把元数据当已发送/已播放。
- 实际 worker 两项交付验收通过（4.253 秒），覆盖 voice/file/both、不发送、QQ 消息和文件
  HTTP 替身、voice 失败仍尝试文件、Job 意图保留、非音频拒绝、篡改拒绝与旧 outbox 默认。
  产物/generation/工具桥/Job/Bot/插件组合回归 102 项通过（153.969 秒）。
- 公共交付前置已提交 `933b85f`。随后先修旧 RVC 协议：每次具名调用使用独立
  session_hash，沿同一会话读取生成器直到 `is_generating=false`，保留最后真实输出而非
  FINISHED_ITERATING 的占位值；请求/响应大小和步骤有界，断连或缺失终态标记失败且不重试。
  真实隔离 UVR 再验收返回 false 终态，两条 6 秒 WAV 均存在；协议/翻唱/本机执行器
  20 项通过（5.153 秒）。这仍是旧入口的 repair pass，新翻唱插件尚未建立。

### 翻唱 A1：RVC 协议单权威与进程安全

- 新建独立 `plugins/akane_cover_song` Python 业务库；尚无插件入口或市场条目。
  原 `cover_song.py` 中的 `RvcWebUiProvider` / `CoverSongError` 实现已删除，
  只保留包导入；内置编排与本机服务都走同一协议实现。
- 迁移窗口：A1 开始，内置 `CoverSongService` 编排暂留；安装包优先，源码部署
  可导入仓库中的同一实现。最终插件 C 切换必须删除内置工具、编排和此源码回退；
  独立本机服务只能保留明确的包加载适配，不能借它激活未安装的模型工具。
- 跨进程 OS 锁覆盖选音色到推理结束；同端口 loopback 别名和 URL 前缀共用保护。
  请求发送前落盘 inflight 标记；只有确认远端终态才能清除。进程被杀、断连或
  终态无效后，新推理结构化拒绝，管理员重启专用 WebUI 后才能显式解除保护。
  这不是新 Job 队列；交互式 WebUI 和第三方客户端不受锁保护，必须使用专用实例。
- 取消不会丢下远端 generator；继续排空同一 session 后才报告取消。断连与取消
  同时发生仍报告未确认失败。返回文件必须位于受信输出目录；未知参数布局、
  路径越界、空音轨均失败。UVR 不再盲重试，工作目录每次唯一。
- `TEMP`/`TMP` 必须在启动 WebUI Python 前指向专用输出目录；旧实例若返回其他
  目录将明确失败，不能为兼容而重新开放任意路径。库没有修改原 RVC 安装。
- 六项真实子进程/HTTP 安全测试通过；原协议、翻唱、本机执行器 20 项通过。
  现有权重的独立真实验收完成 UVR→RVC：两条 1,057,964 字节 WAV，转换输出
  478,444 字节、40 kHz 单声道、5.98 秒；最新实现复验 2.047 秒且保护标记已清。
  没有下载权重、调用收费服务或真实 QQ 投递。混音、缓存、插件安装与生命周期
  仍待本项后续切片，不能把 A1 算成翻唱整项完成。
- A1 wheel 实际构建并安装到忽略的隔离目录，`python -I` 从该安装目录导入
  RVC 库成功，进程未导入 `companion_v01`；不是安装到用户运行实例。

### 翻唱 A2：媒体处理单权威

- 新库 `CoverMedia` 接收异步进程执行端口，独立库不导入宿主/SDK；宿主与本机服务
  的旧解码、时长检查、混音实现删除，适配到同一实现和公共取消安全子进程执行器。
- 混音兼容 RVC 自带 FFmpeg 4.3，不再使用该版本不支持的 `normalize=0`。
  短轨补齐到长轨时长后抵消默认平均，防止其中一轨先结束时剩余伴奏突然放大。
  使用真实波形 RMS 验证音量和尾段；WAV/FLAC/320 kbps MP3 实际输出均有效。
- FFprobe 改查音频流时长，视频比音频长时不误用视频时长；未知/无效时长和缺少
  FFprobe 明确失败，不以零秒绕过输入上限。限定输入协议/容器，去除媒体元数据，
  拒绝覆盖已有输出，取消时等待真实 FFmpeg 退出。
- 包内 12 项通过（6.943 秒），其中新增 6 项真实媒体测试；同样的 6 项在现有
  RVC FFmpeg 4.3 下通过。宿主与本机服务适配输出逐字节一致；旧链路 20 项通过
  （6.689 秒），ruff 与 diff 检查通过。没有运行新收费请求、真实 QQ 或播放。
- 本切片仅完成媒体单权威；两层缓存、整体编排、市场安装/Job/生命周期仍未关闭。

### 翻唱 A3：编排与两层缓存迁入独立库

- `CoverPipeline` 接管源校验/哈希、音色参数键、分离/转换/混音、成品恢复；宿主旧
  `CoverSongService` 现在只解析材料、绑定公共进程端口并登记产物，不再自有编排/缓存。
  本机服务完整 Demucs 请求仍经 provider 接口保留；其客户端/服务端业务后续继续收敛，
  插件入口、安装和最终旧工具删除尚未完成。
- 新缓存按完整 profile 身份 SHA-256 隔离；音频对象只复制不硬链接，内容 SHA-256
  校验后才能恢复；两条分轨由一次原子清单提交同时变为可见，进程竞争不拼接半成品。
  缓存写失败保留真实输出并返回明确 notice；不自动清理可能仍被另一进程读取的对象。
- v2 缓存不删除、不自动导入：旧有损 profile 文件名不足以证明身份，旧清单也没有
  内容校验。已登记产物不受影响；需要原材料重建 v3。仅凭歌名恢复还会核对参数、
  音色和格式；原唱歧义明确失败，强制重做缺少原材料时拒绝，提供材料时跳过两层缓存。
- 异步 provider 适配在重复取消时等待实际线程结束，断连不确定保持 failed；调用者
  负责在 await 完成后清理工作副本。未另建 Job 队列或宿主公共存储权威。
- 包内 19 项通过（13.201 秒）；旧翻唱/协议/本机执行器 20 项通过（6.086 秒）。
  新缓存测试含实际子进程竞争和中断写入；编排用明确替身验证两层语义，混音用真实 FFmpeg。
- 另行真实完整 UVR→RVC→混音验收：新建离线合成语音生成 719,776 字节、5.997275 秒 WAV，
  首次 11.194 秒；按源材料复用和仅按歌名恢复均逐字节一致且不重复推理。
  初次尝试时服务尚未启动就绪，明确返回不可用；待监听后完成真实验收，未忽略失败。
  隔离 WebUI 保护标记已清，受控进程已停止，无新下载/收费请求/真实 QQ/播放。
- wheel 构建后安装到忽略的隔离目录，`python -I` 从新 wheel 导入完整业务库成功且
  没有导入宿主模块。ruff、格式与 `git diff --check` 通过。翻唱整项仍未关闭。

### 翻唱 A4：远端媒体服务客户端单权威

- `RemoteRvcClient` / `RemoteRvcProvider` 进入独立包；宿主的 RVC 模型发现、分轨传输、
  转换和完整翻唱客户端删除，仅继承公共客户端并保留原 ASR 兼容实现。
- 真实 HTTP 请求禁用环境代理/凭据和重定向；响应与 ZIP 解压大小有上限，模型/index
  名称拒绝路径，计时字段白名单，不把远端错误正文或私有路径传给模型。
- 取消必须等待当前请求结束；断线、超时、非 200 响应保留持久保护，后续进程/别名客户端
  不可重试。HTTP 错误也保守保护，因为代理返回错误不证明上游停止；管理员需重启专用
  媒体服务及 WebUI，再按受影响端点分别显式解除保护，健康检查不能代替重启确认。
- 新增六项真实 loopback HTTP 测试通过；包内全部 25 项通过（13.690 秒），旧翻唱/
  协议/本机客户端 20 项通过（7.064 秒）。音频响应是明确传输测试夹具，不声称真实推理。
  服务端 Demucs 编排仍待收敛；插件入口、市场安装、Job/生命周期和旧工具切换尚未完成。

### 翻唱 A5：本机完整翻唱服务复用同一编排

- 本机 `/v1/rvc/cover` 删除自有分离→转换→混音编排与闲置混音入口，改用公共
  `CoverPipeline(cache=None)`；只保留既有 Demucs 部署和 RVC 的适配。服务端不建
  第二份缓存，完整成品复用仍在客户端。独立服务不再经旧内置翻唱服务导入业务库。
- UVR/转换路由改为每次请求独立 provider，不再共享可变分离模型；异步线程适配等待
  当前推理终态再清理临时文件。参数越界明确失败，不再静默夹取参数。
- 新增四项 ASGI 路由测试：真实 FFmpeg 三种格式、无服务端缓存、失败不产假音频、
  重复取消后实际线程结束前工作文件保留，以及模型选择不污染其他请求。
  这些测试的 ML 边界使用明确替身，不当作推理验收。
- 另用已有模型完成真实本机 HTTP 服务→Demucs→RVC→混音→新客户端缓存：6 秒 WAV
  成品 720,102 字节，首次 34.010 秒（分离 24.511、转换 8.056、混音 0.389 秒）；
  两次成品恢复逐字节一致且不重复推理。两个端点保护标记均清除，受控服务已停止。
  没有模型下载、新收费请求、真实 QQ 或播放；插件入口/安装/Job/生命周期仍待后续。
- 包内 29 项通过（18.624 秒），旧翻唱/协议/本机客户端 20 项通过（6.299 秒）；
  wheel 重新构建并隔离安装，`python -I` 导入远端客户端/完整编排且未导入宿主模块。
  ruff、相关新文件格式、语法和 diff 检查通过；保留旧 ASR 段落原样，不顺带格式化。

### 翻唱 A6：旧音频质量与性能 repair pass

- 用户补充要求：旧文件处理/音频转写不视为正确基线，迁移中遇到可验证的性能、逻辑、
  格式问题可顺手修，优先保证音频质量与速度。后续每项仍保持小切片，不以兼容为由照搬错误。
- 真实旧成品经 FFprobe 证实为 40 kHz 单声道：amix 跟随第一条 RVC 人声，把立体声伴奏
  也压成单声道。现两条输入在 amix 前明确协商 44.1 kHz 立体声；WAV/FLAC/MP3 均以
  真实左右不同信号测试，避免仅把已压成单声道的结果“假扩成双声道”。不声称升采样创造音质。
- 新源成品缓存键升为 `rvc-cover-v4-stereo`，分轨缓存仍可复用。历史点播录音照原样恢复，
  不改写或冒称立体声；新结果附真实探测格式，旧远端返回不同格式时明确 notice。
- 本机 Demucs 自己负责预处理，删除翻唱外层重复解码；默认模型复用服务启动时真实 probe
  的格式信息，不再每个请求启动一次额外探测进程。非默认模型仍实际 probe，推理仍验证模型。
- 标题缓存恢复不再要求 RVC 在线。没有显式/默认音色且存在多个候选时明确歧义；在线与离线
  模型匹配同一实现。每个歌/音色只校验最新有效音频，损坏时回退旧有效版本；10 版本夹具从
  10 次哈希降为 1 次。大文件缓存校验/复制/提交移出事件循环，取消仍等待操作收尾。
- 全链路性能样本从前次 34.010 秒到 16.441 秒（分离 9.254 秒），仅单次观察，不保证普遍减半；
  系统缓存/负载不同。随后独立用保留的真实 UVR/RVC 分轨验证立体声混音：WAV 1,586,982、
  FLAC 631,460、MP3 242,459 字节，均为 44.1 kHz 双声道、5.997279 秒，原分轨哈希不变。
  两服务关闭后，旧真实成品 720,102 字节在 0.016 秒内逐字节恢复。
- 包内 33 项通过（24.538 秒），旧翻唱/协议/客户端 20 项通过（7.280 秒）；真实媒体 7 项
  在系统和旧 FFmpeg 4.3 均通过。旧本机服务 5 项真实 CPU/FLAC/CLI/ASGI 回归通过（38.459 秒）；
  含真实 ASR 消费者的合并 23 项通过（36.463 秒）。旧服务测试仍引用已删混音 helper 的遗漏已修，
  从假 subprocess/固定 filter 字符串改为真实音频输出验证。没有新下载/收费请求/真实 QQ/播放。

### 翻唱 B：独立插件、真实安装与 Job 验收

- 新 `akane.cover-song.run.v1` 从真实 wheel 进入市场/安装/独立进程；只用公共 SDK 的资源、
  命名连接、存储与产物契约。`connection.rvc.read` 投影现有 COVER_SONG/RVC 配置，不另建
  配置仓库，不向 prompt/结果暴露端点、模型目录或其他凭据。健康检查不访问调用期连接。
- 当前用户身份来自可信调用上下文，在实例/插件存储下再次隔离缓存；默认 QQ 请求 voice、
  其他客户端请求 file，none 只登记，both 为一件产物的双交付意图。每次恢复重新探测真实音频。
- 中风险插件遵循首次确认；旧内置的 medium/never 不再复制。未替用户修改安装或审批配置。
- 两个真实安装测试通过（48.699、48.522 秒）：缓存/重做/用户隔离/禁用启用卸载，重复取消
  等待真实终态、断连保留 unconfirmed 失败、原 Job/MemCore/桌面事件/QQ 适配器。HTTP 音频与
  QQ 发送使用明确夹具，不能当作模型质量或真实消息验收。命名连接回归 7 项通过（12.779 秒）。
- 独立真实模型验收：隔离安装后由 HostJob 驱动既有离线 Demucs/RVC，6 秒自制合成音频得到
  1,587,702 字节、44.1 kHz 双声道 WAV；Job 30.218 秒，两次恢复后总计 30.797 秒，字节完全一致。
  两个验收服务均已停止；无模型下载、收费请求、真实 QQ 或播放。
- B→C 迁移窗口仅保留旧内置装配待下一切片删除；权威算法已全部在新包。删除目标为旧 handler、
  ToolSpec/schema/catalog、固定翻唱提示、engine factory、宿主 cover service 与 RVC 客户端兼容层；
  不继续扩展旧入口。整项在 C 删除和双模式发现验证后才关闭。

### 翻唱 C：移除旧入口和宿主业务依赖

- 删除 `companion_v01/cover_song.py`、旧 handler/ToolSpec/重复 schema/metadata/catalog、
  engine 工厂与固定翻唱提示、快照缓存标记和每轮缓存探测；不留下假可用/待恢复的翻唱提示。
- 旧媒体客户端不再继承 RVC 类或源码 fallback；保留原 ASR 消费者与上传 suffix 工具。
  RVC 模型文件卡片移入独立库，服务仅绑定该公开函数。宿主运行时不导入翻唱业务包。
- 双模式真实安装测试确认未装/禁用/卸载不可见，安装后 native/兼容同源；QQ auto 的实际
  产物事件为 voice，both 仍仅登记一件文件。原 QQ voice/both 隐私回归保留，旧假 service
  测试由独立库真实媒体/缓存与安装 Job 测试替代。没有修改 main/settings/CSS/布局。
- 24 项安装/发现/唯一权威/原生 schema 通过（98.871 秒）；包内 33 项通过（24.696 秒）；
  154 项协议/装配/工具调用/原生通道通过（10.933 秒），153 项路由/VN 通过（37.418 秒）。
  各组合存在重叠，不累加成独立测试总数。旧服务删除可由 git 历史恢复，用户文件和缓存未删除。
- 原本机媒体服务与实时 ASR 消费者的 8 项真实回归通过（96.766 秒），包含 CPU 分轨、
  FFmpeg/FLAC、CLI/ASGI 和真实语音识别；没有为了移除翻唱依赖删掉语音产品入口。

### 翻唱交付后音频边界 repair

- 新测试先复现同名模型/同参数切换服务命中旧缓存。现源缓存键增加不泄露路径的服务/根目录
  标识与本地 UVR 权重 stat，改变服务或所跟踪的分轨权重会同时失效两层源缓存；歌名点播仍
  恢复历史录音，不要求原服务在线。旧远端协议无法观测原地替换分轨权重，管理员操作后需重做。
- FFprobe 真实容器/编码/采样率/声道/时长验证集中到唯一流水线，新成品和缓存恢复均执行。
  WAV 冒作 MP3 返回明确 `cover_output_format_mismatch`，不登记或缓存；插件层重复 probe 删除。
- 包内 36 项通过（22.694 秒），新增服务标识/权重变更、错扩展名实际探测及两层缓存失效检查。
- 最终两项真实安装/生命周期/Job 测试通过（103.568 秒），使用 audio 类型附件和生成句柄，
  覆盖跨会话拒绝及真实 WAV 响应被错误请求为 MP3 时无产物登记；缓存专项 11 项再通过（9.983 秒）。
- 新 wheel 再隔离安装并真实直连 WebUI：HostJob 驱动 UVR/RVC，5.997279 秒自制素材得到
  1,586,982 字节 24 位 WAV，44.1 kHz 双声道；Job 7.891 秒、两次恢复后 8.422 秒，字节一致。
  这是单次观察，不保证普遍性能或主观唱腔质量；验收 WebUI 已停止，无下载、收费、QQ 或播放。

### 文档 A：独立渲染库与保真 repair

- 新增独立 `akane-document-writer` 库，目前为 incubating，尚未绑定宿主或加入市场；
  旧入口仍是现有运行时入口，不在这里宣称文档插件已完成。B 接公共 SDK/真实安装，
  C 删除 compose/revise/style 旧业务及固定提示后，才可关闭第七项。
- 表格不再吞掉 `0`、`False`、空行或空白；Excel 保留数字/布尔类型，字符串不被隐式执行
  为公式。CSV 保留原值并提示公式样文本的导入风险。参数超限拒绝，不静默截断。
- 共用严格样式定位：保留 column_index/row_start/row_end，拒绝负数、越界、模糊匹配、
  重复表头、字符串布尔值、无匹配与不支持的规则；Word 段落序号真正进入运行链，超链接
  run 同样收到样式。任意 RGB 使用真实 shading，不再统一降为黄色或叠加重复 shading。
- Word/PDF 共用小型 Markdown 解析，标题不会吞掉紧随的正文，表格保持真实结构与零值，
  显式编号保留。样式另存新文件，旧文件字节不变；多表/多工作表要求明确选择。复杂签名、
  宏/嵌入对象与 openpyxl 明确警告将丢失的特性拒绝处理，避免假装保真。
- PDF 页面检查发现 CID 替换字体造成英文间距异常，已改为校验本机可嵌入 TrueType 字形，
  嵌入实际子集。无字体/缺字明确失败；没有字体下载或再分发。当前只用单字体面，尚未提供
  真正的粗体/斜体变体和任意文字 shaping。PDF 单页中文/英文/列表/表格样本已重新渲染核对。
- 22 项真实文件测试在捆绑 Python 通过（1.732 秒），产品 Python 再通过（1.612 秒）；
  覆盖实际 docx/xlsx/pdf 读回、源文件哈希、公式保留、越界失败和无半成品。Excel 原版/样式版
  用 Artifact Tool 只读导入与渲染核对；该预览器将布尔显示为 0/1，实际文件与读取结果仍为 bool。
- Word 结构检查通过，但 `render_docx.py` 实际失败：无 LibreOffice/soffice.exe。捆绑运行时
  与常见安装目录均无该程序，已请求临时依赖下载许可；未擅自下载。Word 逐页视觉验收仍未完成，
  不把 XML 检查或 PDF 样本冒充 Word 渲染验收。

### 文档 A2：补齐 Word 页面验收

- 用户明确批准临时下载后，从官方列出的南京大学镜像下载 LibreOffice Portable 26.2.4，
  223,657,592 字节；官方 SHA-256 为
  `4bde93374aef4409243505b20d16561a4628ac7591457dd01fc6e1ccf571ba65`，本地校验一致，
  Authenticode 签名有效。仅解压到忽略的任务临时目录，没有执行系统安装或修改文件关联。
- 文档技能的真实渲染检查发现默认模板 Title 的蓝色下边框，已在新建文档时移除该残留，
  同时去掉覆盖显式字体的 theme 引用并禁用默认下划线；样式修改既有文件仍保留源设计。
- 使用该隔离 LibreOffice 和捆绑 Python/Poppler 运行 `render_docx.py`，重渲染原版与样式版
  各一页，再检查 48 行、3 页长表。五页全部视觉核对：无蓝线、无裁字、中文和数值完整，
  样式定位正确、长行自动增高、后续页重复表头。22 项文件测试再次通过（2.190 秒）。
- 原 Word 渲染环境阻塞已解除；程序/样本留作后续端到端验收，不进入仓库。首次 `--version`
  留下的两个自有启动进程已核对命令行后停止；实际渲染进程正常退出，无遗留进程。

### 文档 B1：公共产物来源和版本契约

- 公共 SDK 产物草稿增加可选 `source_handles`、`revision_of`，默认为空，旧插件与旧 outbox
  保持兼容。来源只接受有限的现有句柄，不接受文件路径、latest 或插件指定的版本号。
- 宿主按可信用户/会话解析来源，在原 GeneratedFileStore 中登记父版本和递增版本号；
  独立进程 outbox 完整传递并核对来源字段。需要 resource.read，跨会话/用户引用明确失败。
- 30 项来源/产物/资源测试通过（23.969 秒）：真实文件存储保留旧字节、连续版本和来源链，
  篡改 outbox 被拒绝，旧字段缺省兼容，原资源独立进程及取消清理回归通过。文档插件的新字段
  独立进程端到端验证仍待 B2，当前没有把新文档能力冒称已安装或已接通。

### 文档 B2 前置：完整来源表示

- 资源端口增加可选 `representation=document`，由宿主在独立、限时进程中解析当前会话材料，
  返回调用期私有 JSON；旧 `original` 仍返回逐字节副本，不改旧插件默认行为。
- 正文/表格保持出现顺序，工作表全部读取，数字/布尔值保留类型，公式和日期带明确类型；
  未覆盖的图片、批注、编号、合并单元格等标记 `complete=false`，不宣称完整保真。
  超预算、损坏、编码未知或缺依赖明确失败，不用预览或错误字符串冒充正文。
- 来源解析和句柄权限仍由宿主拥有，插件不导入该解析器。反向回调在父进程再次检查
  resource.read。真实 worker 已验证附件/生成物读取、跨会话拒绝和工作目录清理；真实慢解析
  子进程的重复取消验证了先退出再删除临时文件。此时文档市场能力仍待下一切片接入。
- 来源/资源/版本组合 23 项通过（25.733 秒），补充空白与加密 PDF 后解析专项 8 项通过
  （0.903 秒）；已检查此前真实 Word/Excel/PDF 样本的解析覆盖标记。含列表编号的 Word
  如实标为不完整，未把“提取到了文字”等同于保留了全部编号及版式。

### 文档接入期间的只读资源修复

- 发现旧 sendable resolver 在读取生成物时也更新 delivery_status=pending 和 updated_at，
  会把参考原稿误标为待发送并改变“最近文件”顺序。现状态更新只留在明确的 send_file 路径，
  资源打开、来源解析和版本验证保持只读，未另建解析器或额外状态。
- 生成物读取专项与原 GeneratedFileService 组合 36 项通过（20.061 秒），覆盖显式句柄、
  内部生成物 ID、latest_generated/latest 读取不变，以及真正发送仍标为 pending。

### 文档 B2：可安装插件与真实成品验收

- 市场新增 `akane.document-writer`，通过公共 SDK 提供 compose/revise/style 三项长任务。
  不读取宿主私有服务；依赖缺失明确失败，健康检查真实生成、修改和读回 Word/Excel/PDF。
  渲染子进程可取消并等待退出，注册与渠道发送保持不同事实。
- 新建需要完整正文/表格；原文导出显式使用 full_text，拒绝不完整提取、混合多表静默丢弃
  和无时间信息的字幕转换。原文中的 `#`、`**`、管道符不被当成新 Markdown 重解释。
  修订和样式修改使用真实公共来源/版本契约，旧文件不覆盖、不误标待发送。
- 28 项库测试通过；两项真实 wheel 安装测试通过（124.865 秒），覆盖 11 格式读回、
  长来源全文、新旧版本、现有公式、多工作表定点样式、12 类失败、权限和禁用/启用/卸载。
  真实 HostJob 验证幂等、失败、重复取消、子进程退出和无半成品；桌面事件与 MemCore 实际
  投影、QQ 模拟 transport 的文件交付通过，未进行真实 QQ 投递。
- Windows 的 CSV 文件关联曾令标准 text/csv 被拒，现使用固定标准 MIME；17 项公共产物
  测试通过，包括系统 MIME 猜测错误时接受标准类型且拒绝伪装类型。
- 再实际安装插件生成 8 件验收成品。按文档/PDF/表格技能只读渲染核对 7 页 Word、1 页 PDF
  和两张 Excel 预览：48 行表格三页连续、续页重复表头、长行完整、样式定位正确、中文无缺字。
  Excel 预览器将布尔值显示为 0/1，原 XLSX 与类型检查仍为布尔值，未为预览修改原数据。
  这些预览均来自真实插件产物，不是另做演示稿。C 仍需删除宿主旧文档业务后关闭第七项。

### 文档 C：移除旧业务、完成第七项

- B2 提交 `b15d1e9` 后删除 `generated_files_io.py`、三个旧业务方法/处理器/ToolSpec、
  catalog/metadata/engine 绑定、会话文档生成模块、样式校验与正文补全/card/followup。
  旧附件“较长预览生成”接口同步移除；宿主不导入新文档业务库，也不保留调用旧函数的兼容壳。
- `GeneratedFileService` 仅保留文件存储、来源/版本登记、查看、发送与 ASR 产品薄绑定。
  媒体查看仍使用的来源解析器保留并改为媒体名称；没有删掉媒体查看或实时语音消费者。
  最后一批无调用者的旧 FFmpeg 命令构造/媒体参数 helper 同步删除。
- 通用测试改用真实中立文本文件登记，不重建假 compose 服务。发送/管理的目标解析不再
  借用 compose 处理器；空的旧“强制取消自动发送”名单与包装函数删除，插件仍按真实交付意图运行。
  旧启动必需项中的 compose/image 移除，避免可选插件缺席误报内置能力故障；控制中心仅去掉
  已退役名称分类，更新只读 probe fixture，未改 main/settings/CSS/布局或用户设置。
- 新增唯一权威源码/行为守卫；实际安装测试确认两个渠道 native/兼容提示中始终没有旧三项，
  新三项严格随安装/禁用/卸载出现和消失。内置 canonical 工具从 44 项变为 41 项。
- 补充失败先行测试复现 LRC 被生成物查看当作二进制；修复中立文本查看类型后可读取歌词。
  插件测试还逐一通过宿主查看七种文本格式，不只检查原始输出字节。
- 验证：独立库 28 项（1.593 秒）；插件安装/来源/公共产物组合 30 项（148.092 秒）；
  文件/资源/版本/装配/发布门槛组合 154 项（44.763 秒）；路由/VN/原生工具/边界组合
  224 项（43.256 秒）。这些组存在重叠，不累加为独立测试总数。
- QQ/桌面文件与交付组合 88 项（36.227 秒），87 通过、1 项因 Windows 无 symlink 创建能力
  跳过；没有真实 QQ 投递。桌面 `build`、`smoke:control-center-actions`、
  `smoke:control-center-v2`、`probe:control-center-runtime` 全部通过。
- LRC 补充后的实际安装/全格式/来源/修订/样式/生命周期测试再次通过（68.418 秒）；
  本机媒体客户端/真实服务与 CLI/插件市场/安装生命周期组合 19 项通过（75.454 秒）。
  所有改动 Python 模块的未定义符号检查、语法编译，新插件与新增测试 lint/format、
  `git diff --check` 通过。
- 扩大到编码项目 smoke 的 235 项组合有两项失败：Windows 命令连接符与引号不匹配当前
  PowerShell。已比较 HEAD 的测试 AST 一致，再直接运行 HEAD 两个原 smoke，复现同样失败
  （只移除未使用的退役描述符导入）。执行器未改；这是单列的既有失败，不标成全绿或本轮修复。
- 旧业务代码可由 git 历史恢复；没有删除用户文件、数据库、模型权重或缓存。

## 最终交付与授权边界

- 全部七项逐项关闭，才可完成整个目标。每项报告提交、真实命令/结果、用户能感受到的变化和仍未实测部分。
- 公网市场发布、云端部署、真实 QQ 投递、需要新授权的付费服务或重大环境部署不自动执行。替身验证和真实外部验收分别记录。
- 不修改桌宠 main/settings/CSS/布局；原有角色文档、角色资源测试及 `work/` 不夹带进提交。
- V1 扩展回归已有两个 MCP 家族禁用测试在基线失败；不混入本目标，若相关行为因本轮变化再受影响则重新核查。
