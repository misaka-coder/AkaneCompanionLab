# 桌宠附件对齐：原生边界 repair pass

2026-09-07，第一切片。

- `import_dropped_files`、`open_workspace_item` 的 JS 调用按 Tauri 默认 camelCase
  解码传参，覆盖立绘拖入、工作台打开/定位/导出、音频暂存和自动成果交付。
- 文件请求绑定当前已验证后端和选中 Bot；原生端核对持久绑定，走 Bot 作用域路由，
  不再无条件请求启动环境的默认后端/默认 Bot。
- 连续拖入顺序排队，失败不毒化队列；切 Bot、后端、角色、会话或身份后，旧的
  排队请求取消，不把旧结果展示到新会话。已派发的上传不会冒充已撤销。
- 音频只放行实例托管的 `attachments/audio` 缓存目录，不开放拖入源目录。
- 原生读文件/上传逐项失败仍保留先前成功项；返回 `skipped_count` 与安全原因。
- multipart 暂存采用独立子目录保留原文件名，并转交内层 skipped 明细。

验证：队列/作用域/IPC 参数/部分失败 JS smoke；19 项工作台与上传 Python 测试；
43 项桌宠前端与真实媒体插件交付测试；37 项 Rust 测试（含真实本机只读能力）；
Vite build；`git diff --check`。

本切片尚未部署/实测系统拖拽与扬声器出声，不把离线替身测试当实机成功。
当前轮附件输入、图片原图直看、音频作为材料的入口、控制中心附件/富媒体和
普通长任务追加属于后续切片，不在本次修复中宣称已完成。

## 当前轮附件后端切片

- 桌面本地导入图片不再启动旧视觉摘要观察器；图片仍通过共享 Inbox 落盘和记事件。
- 普通同步/流式聊天接收 `current_attachment_ids`，只解析当前身份、会话、角色的
  精确 ID。路径、模糊 latest、其他角色或会话的 ID 不可绑定到当前轮。
- 原图准备复用共享 `prepare_native_image_inputs`；旧 QQ 方法只保留薄适配。
  文件材料标签进入当前用户尾部上下文，原图进入实际多模态输入，附件 ID 写入消息元数据。
- 本轮 `inspect_attachment(latest)` 优先遵循明确绑定，不被更新的工作台材料抢走。
- 视觉不可用保留材料绑定并给出未载入提示，不伪称看过图片；音频标签不代表转写或播放。

验证覆盖真实导入/Inbox 到同步与流式主链路、作用域拒绝、材料工具、原图路由，
以及同一模型下变更附件只改变 provider 消息尾部、不改变稳定前缀与缓存键。
这证明请求结构保持前缀，并不代表远端 provider 实际命中 KV 缓存。
前端待发送区和长任务附件追加尚未连接，仍不宣称实机闭环完成。

## 待发送区与追加消息切片

- 立绘拖入、控制中心添加/拖入/粘贴共用主窗口的 scoped 附件草稿和上传队列。
  卡片返回真实附件 ID；上传中不能先发出缺附件的消息。上传失败、重复操作和
  切身份/会话/角色/后端有明确处理，不把旧草稿恢复到新作用域。
- 音频默认作为材料待发送，只有点击播放才启动音乐；重复导入复用同角色的相同
  内容，按 SHA-256 比较，不仅比较名字和大小。接收失败不返回假成功。
- 正常消息和忙碌中的追加都携带附件 ID；宿主在安全边界重新解析其作用域，
  记录引用并向后续模型轮次提供原始图片。持久队列仍保存引用，不保存图片副本。
- 控制中心忙碌时同时展示追加与停止；实时刷新保留输入焦点和选区，作用域切换
  清空旧草稿；聊天滚动恢复使用即时定位，避免平滑滚动被下一次刷新重置。
- 桌宠附件卡片加入原生点击区域，避免按钮看得见却点穿到后面的窗口。

验证：60 项最新 Python 回归、早先 66 项协调/主链路回归、真实媒体插件安装/转换
与桌面交付消费者测试；37 项 Rust 测试；附件/上传队列/控制中心 JS smoke；
Tauri release build。UX 静态 smoke 的截图项因无 puppeteer 跳过，未计为截图验证。
实机已查看新版聊天布局、打开并取消文件选择器；未上传用户文件或宣称扬声器已出声。
2026-09-07 已同步重启本机后端与新版桌宠。后续仍需验证实际上传/看图/播放/交付，
并补齐控制中心的成果卡片和历史附件展示；群聊主人修复已随本次后端重启加载。

追加实机复核：聊天页初次进入显示最新消息，打开/取消原生文件选择器后仍停留在
相同最新消息位置；没有上传文件，没有点击屏幕共享权限弹窗。重复音频返回已有
材料卡并进入待发送区，不再把复用项显示为“不支持”或“跳过”。

## 历史附件与成果卡片切片

- 会话读取和更早消息分页按元数据中的附件 ID 回读同一材料库，用共同的身份/
  会话/角色校验生成公开卡片；不保存重复索引，不加载原图或额外调用视觉模型。
  失效或越作用域的引用只显示“附件不可用”，不提供其他角色的名称或可操作 handle。
- 聊天页展示历史附件及当前会话最近 12 个生成成果，支持查看/打开、定位、保存
  到桌面，音频支持明确点击播放。成果来自当前会话重新读取，不借用总览旧快照。
- 操作经过控制中心桥及主窗口，复用原生文件交付校验。结果与操作 ID 对应，
  播放需核对实际播放器状态；原生缓存和导出路径不进入操作结果快照。
- 满额新草稿遇到旧请求失败时不再抛出恢复异常；保留新草稿并告知原材料仍在工作台。

验证：152 项 Python 回归通过（含真实导入到会话/分页卡片）；JS 生产数据源、
渲染、文件操作、失败/作用域/确认关联测试及 90 项控制中心桥动作通过；release
构建通过。实机聊天页加载与空成果状态正常，但当前会话没有新附件/成果，尚不能
据此宣称实机卡片点击或扬声器出声通过。图片目前点击后由系统打开，不含聊天内
缩略图；真实上传、远端看图和扬声器播放仍需使用明确获准的测试材料验收。

## 聊天内图片预览切片

- 待发送图片、历史图片及图片成果可点击“在聊天中预览”；原来的系统打开入口保留。
  预览只在当前窗口使用临时 Blob URL，不写库、快照持久层或模型上下文。
- 复用既有 ArtifactBroker 下载响应，校验实例、MIME、字节数和 SHA-256；禁止跳转
  和外部材料 URL，只接受不超过 8MB 的 PNG/JPEG/WebP/GIF/BMP 位图。
- 当前窗口最多保留 6 个预览；重复点击不重复读取。切后端/Bot/身份/会话/角色、
  收起或窗口销毁时取消读取并释放 URL；解码失败、超时和大图有明确提示。
- 51 项 Python 回归（含真实材料导入及下载响应交给生产 JS 消费者）通过；JS 覆盖
  篡改哈希/MIME/大小、跨实例、作用域切换、重复点击、URL 释放和失败渲染。
- 隔离浏览器页面使用生产组件/CSS 与合成图片，已查看明暗主题及过大失败态的实际
  渲染。该页面所有文件操作禁用，未上传用户文件，不冒充本机桌宠真实收发验收。
