# Character Assets

Put portrait and expression assets here:

```text
assets/
  characters/
    default/
      normal.png
      happy.png
      thinking.png
```

The runtime expects `assets/characters/<outfit>/<emotion>.png`.
