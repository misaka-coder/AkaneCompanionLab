# Assets

The Akane sample currently reuses the bundled portrait files in:

`desktop_pet_next/src/assets/characters/猫娘/`

The Creator Kit will later support copying a full character asset set into this
folder and validating it directly from the pack.
