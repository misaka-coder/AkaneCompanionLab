# Character Portraits

Create one folder per outfit, then put expression images inside it:

```text
characters/
  猫娘/
    正常.png
    开心.png
    思考中.png
```

This sample pack intentionally leaves the image files out to avoid duplicating
the bundled demo assets. The runtime falls back to the bundled portraits until
you add images here.
