# Mika Sample Assets

Put portrait images under:

```text
assets/characters/<outfit>/<emotion>.png
```

Example:

```text
assets/characters/default/normal.png
assets/characters/default/happy.png
```

This sample intentionally ships without images so the desktop app can exercise
the bundled fallback path while still validating a second character pack.
