# Character Portraits

Create one folder per outfit, then add one image per emotion:

```text
default/
  normal.png
  thinking.png
  happy.png
```

Supported image extensions: `.png`, `.jpg`, `.jpeg`, `.webp`.
