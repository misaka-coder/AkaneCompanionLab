{
  "schema_version": 1,
  "bots": {
    "personal": {
      "required": [
        "tool.compose_file",
        "tool.send_file",
        "tool.generate_image",
        "provider.tts.edge"
      ]
    },
    "finance": {
      "required": [
        "tool.web_search"
      ]
    }
  }
}
